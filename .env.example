# =============================================================================
# NoteHub Environment Configuration - Hetzner VPS Deployment
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit the .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Flask Application Settings
# -----------------------------------------------------------------------------
# Generate a secure secret key: openssl rand -hex 32
FLASK_SECRET=your-super-secret-key-change-this

# Admin credentials (CHANGE THESE!)
NOTES_ADMIN_USERNAME=admin
NOTES_ADMIN_PASSWORD=YourSecurePassword123!

# -----------------------------------------------------------------------------
# MySQL Database Configuration (Local Container)
# -----------------------------------------------------------------------------
# These credentials are for the MySQL container running on your Hetzner VPS

MYSQL_ROOT_PASSWORD=change-this-root-password
MYSQL_USER=notehub
MYSQL_PASSWORD=change-this-password
MYSQL_DATABASE=notehub

# -----------------------------------------------------------------------------
# Cloudflare Tunnel Configuration
# -----------------------------------------------------------------------------
# Get your tunnel token from the Cloudflare Zero Trust dashboard:
# 1. Go to https://one.dash.cloudflare.com
# 2. Navigate to Networks → Tunnels
# 3. Create a tunnel and copy the token

CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here

# -----------------------------------------------------------------------------
# CAPTCHA Configuration (Optional)
# -----------------------------------------------------------------------------
# Options: "simple" (built-in math captcha), "recaptcha", "none"
CAPTCHA_TYPE=simple

# If using Google reCAPTCHA:
# RECAPTCHA_SITE_KEY=your-site-key
# RECAPTCHA_SECRET_KEY=your-secret-key
